# リポジトリ構造定義書テンプレート

このテンプレートには2種類のフォーマットが含まれる:

1. **index.md**: トップレベル構造・共通ルール・特殊ディレクトリ
2. **{stack}.md**: スタック固有のディレクトリ詳細

---

## index.md テンプレート

```markdown
# リポジトリ構造定義書

## プロジェクト構造（トップレベル）

[プロジェクト全体のトップレベルディレクトリをツリー形式で記載]

## スタック一覧

| スタック | ディレクトリ | 役割 | 詳細ファイル |
|---|---|---|---|
| [スタック名] | [パス] | [役割の概要] | [stack].md |

## 特殊ディレクトリ

### .issue/ (issue固有ドキュメント)

**役割**: 各issueのライフサイクル全体に関わるドキュメントを保持

**構造**:

.issue/
└── {issue番号}/
    ├── （実装前）変更仕様書、影響範囲分析
    ├── （実装中）作業リスト（タスク分解・進捗管理）
    └── （実装後）レビュー結果、品質検証ドキュメント

### .claude/ (Claude Code設定)

**役割**: Claude Code設定とカスタマイズ

**構造**:

.claude/
├── commands/                # スラッシュコマンド
├── skills/                  # タスクモード別スキル
└── agents/                  # サブエージェント定義

### docs/ (ドキュメントディレクトリ)

**配置ドキュメント**:

- `product-requirements.md`: プロダクト要求定義書
- `functional-design.md`: 機能設計書
- `data-model.md`: データモデル設計書
- `screen-specification/`: 画面仕様書
- `architecture/`: アーキテクチャ設計書
- `repository-structure/`: リポジトリ構造定義書（本ドキュメント）
- `development-guidelines/`: 開発ガイドライン
- `design-patterns/`: デザインパターン
- `glossary.md`: 用語集

## 除外設定

### .gitignore

プロジェクトで除外すべきファイル:

- `node_modules/`
- `dist/`
- `.env`
- `.issue/` (issue固有ドキュメント)
- `*.log`
- `.DS_Store`

### .prettierignore, .eslintignore

ツールで除外すべきファイル:

- `dist/`
- `node_modules/`
- `.issue/`
- `coverage/`
```

---

## {stack}.md テンプレート

```markdown
# [スタック名] リポジトリ構造

## ディレクトリ構造

[スタック固有のディレクトリ構造をツリー形式で記載]

## 依存ルール

### レイヤー間のインポート制約

[依存関係の図を記載]

| レイヤー | インポート可能な対象 | インポート禁止の対象 |
|---|---|---|
| [layer1] | [対象] | [対象] |
| [layer2] | [対象] | [対象] |

### モジュール間のインポートルール

[フィーチャー間等のインポートルールを記載]

## ディレクトリ詳細

### [ディレクトリ名] ([役割])

**役割**: [説明]

**配置ファイル**:

- [ファイル種別とその説明]

**命名規則**:

- [ルール]

**依存関係**:

- 依存可能: [対象]
- 依存禁止: [対象]

**例**:

[ディレクトリ構造の例]

### テストファイルの配置

**単体テスト・統合テスト**: テスト対象ファイルと同じディレクトリに配置

- 単体テスト: `[テスト対象].test.ts`
- 統合テスト: `[テスト対象].integration.test.ts`

**E2Eテスト**: `e2e/` ディレクトリに配置

- パターン: `[シナリオ名].test.ts`（kebab-case）

## ファイル配置規則

| ファイル種別 | 配置先 | 命名規則 | 例 |
|---|---|---|---|
| [種別1] | [パス] | [ルール] | [例] |

## 命名規則

### ディレクトリ名

- [ルール]

### ファイル名

- [ルール]

## スケーリング戦略

### 機能の追加

新しい機能を追加する際の判断基準:

1. [基準1]
2. [基準2]

### 肥大化への対応

[分割・リファクタリングのルール]

### ファイルサイズの管理

- 1ファイル: 500行以下を推奨
- 500-800行: リファクタリングを検討
- 800行以上: 分割を強く推奨
```
