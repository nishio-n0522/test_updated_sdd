# 変更仕様書: [変更概要を1行で記述]

| 項目        | 内容                          |
| ----------- | ----------------------------- |
| 作成日      | [YYYY-MM-DD]                  |
| 影響画面    | [SCR-XXX, SCR-YYY, ...]       |
| 関連PRD機能 | [F番号（該当する場合）/ なし] |

## 変更の背景

[なぜこの変更が必要か、ユーザーの指示内容を1〜3文で記述]

---

> 複数画面に変更がある場合は、「## SCR-[番号]: [画面名]」から次の「---」までの
> ブロックを画面数分繰り返す。変更サマリはBefore/Afterを作成した後に記入する。

## SCR-[番号]: [画面名]

### 変更サマリ

- **追加**: [新たに加える要素・操作を箇条書き（該当なしの場合は「なし」）]
- **変更**: [既存の要素・操作の変更点を箇条書き（該当なしの場合は「なし」）]
- **削除**: [除去する要素・操作を箇条書き（該当なしの場合は「なし」）]

### Before（現在の仕様）

> 出典: `docs/screen-specification/scr-[番号]-[name].md`

> 変更に関連する部分のみを抜粋する。変更対象箇所の行末に「← ★変更対象」を付ける。

#### ASCIIワイヤーフレーム

```
[変更に関連する部分のASCIIワイヤーフレームを抜粋]
```

#### UI要素定義（変更対象のみ抜粋）

| #   | 要素名   | 種類   | 説明   | 初期値 | 制約・条件 |
| --- | -------- | ------ | ------ | ------ | ---------- |
| [#] | [要素名] | [種類] | [説明] | [値]   | [制約]     |

#### 操作挙動仕様（変更対象のみ抜粋）

**操作: [操作名]**

**トリガー**: [ユーザーのアクション]
**前提条件**: [操作が有効な条件]

**正常系の挙動**:

1. [ステップ1]
2. [ステップ2]
3. [ステップ3]

**異常系の挙動**:

| 条件        | 挙動           |
| ----------- | -------------- |
| [異常条件1] | [対応する挙動] |

**画面遷移**: [遷移先 / 遷移なし]
**状態変化**: [操作前] → [操作後]

### After（変更後の仕様）

> 変更後の状態を画面仕様書と同一フォーマットで記述する。
> 追加箇所の行末に「← ★NEW」、変更箇所の行末に「← ★CHANGED」を付ける。

#### ASCIIワイヤーフレーム

```
[変更後のASCIIワイヤーフレーム]
```

#### UI要素定義（追加・変更する要素）

| #   | 要素名   | 種類   | 説明   | 初期値 | 制約・条件 |
| --- | -------- | ------ | ------ | ------ | ---------- |
| [#] | [要素名] | [種類] | [説明] | [値]   | [制約]     |

#### 操作挙動仕様（追加・変更する操作）

**操作: [操作名]**

**トリガー**: [ユーザーのアクション]
**前提条件**: [操作が有効な条件]

**正常系の挙動**:

1. [ステップ1]
2. [ステップ2]
3. [ステップ3]

**異常系の挙動**:

| 条件        | 挙動           |
| ----------- | -------------- |
| [異常条件1] | [対応する挙動] |

**画面遷移**: [遷移先 / 遷移なし]
**状態変化**: [操作前] → [操作後]

---

> 新規画面が追加される場合のみ以下の「## 新規画面」セクションを出力する。
> 該当しない場合は「## 新規画面」セクション全体（次の「---」まで）を省略する。
> 新規画面の仕様は `.claude/skills/screen-specification/templates/screen.md` を
> Read で読み込み、そのフォーマットに従って完全な画面仕様を記述する。
> テンプレート内の指示ブロック（「> 作成する状態...」等）は出力しない。
> 最低限含むべき要素:
>
> - ファイルヘッダー（仕様ファイルパス、対応PRD機能）
> - メタデータテーブル（作成日、最終更新日、参照ドキュメント）
> - 概要
> - ASCIIワイヤーフレーム（初期状態 + 必要な状態別）
> - Markdownワイヤーフレーム（UI要素定義）
> - 操作挙動仕様

## 新規画面

### SCR-[番号]: [新画面名]

**Before**: 該当画面なし - 新規追加

**After**:

[screen.md テンプレートのフォーマットで完全な画面仕様を記述]

---

## index.md への影響

### 画面一覧の変更

- [追加・変更・削除される画面の一覧。変更なしの場合は「変更なし」]

### 画面遷移図の変更

- [追加・変更・削除される遷移の一覧。変更なしの場合は「変更なし」]

### 画面遷移一覧の変更

- [追加・変更・削除される遷移の詳細。変更なしの場合は「変更なし」]
