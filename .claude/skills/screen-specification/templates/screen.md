# SCR-[番号]: [画面名]

> 仕様ファイル: `docs/screen-specification/scr-[番号]-[name].md`
> 対応PRD機能: F[番号], F[番号]

| 項目         | 内容         |
| ------------ | ------------ |
| 作成日       | [YYYY-MM-DD] |
| 最終更新日   | [YYYY-MM-DD] |
| 参照ドキュメント | index.md, `docs/functional-design.md` |

## 概要

[画面の目的と役割を1〜2文で記述]

## ASCIIワイヤーフレーム

### 初期状態

```
┌──────────────────────────────┐
│ [タイトル]            [操作]  │  ← ナビゲーションバー
├──────────────────────────────┤
│                              │
│  ┌──────────────────────┐   │
│  │                      │   │
│  │  [入力エリア]         │   │  ← [説明]
│  │                      │   │
│  └──────────────────────┘   │
│                              │
│  ┌──────┐ ┌──────┐ ┌──────┐ │
│  │ [Btn] │ │ [Btn] │ │ [Btn] │ │  ← [説明]
│  └──────┘ └──────┘ └──────┘ │
├──────────────────────────────┤
│  [Tab1]  [Tab2]  [Tab3]      │  ← タブバー
└──────────────────────────────┘
```

### [状態名]（例: 入力中、データあり、空状態、エラー）

> 作成する状態ワイヤーフレームの決定基準:
> - 入力フィールドがある → 「入力中」「バリデーションエラー」
> - 一覧表示がある → 「データあり」「空状態」「ローディング」
> - 非同期処理がある → 「ローディング」「エラー状態」
> - 条件による表示切替がある → 各条件の状態
>
> 該当する状態のワイヤーフレームを選択して記述してください。

```
[状態別のASCIIワイヤーフレーム]
```

## Markdownワイヤーフレーム（UI要素定義）

| #   | 要素名   | 種類   | 説明   | 初期値 | 制約・条件 |
| --- | -------- | ------ | ------ | ------ | ---------- |
| 1   | [要素名] | [種類] | [説明] | [値]   | [制約]     |
| 2   | [要素名] | [種類] | [説明] | [値]   | [制約]     |
| 3   | [要素名] | [種類] | [説明] | [値]   | [制約]     |

## 操作挙動仕様

### 操作: [操作名1]

**トリガー**: [ユーザーのアクション]
**前提条件**: [操作が有効な条件]

**正常系の挙動**:

1. [ステップ1]
2. [ステップ2]
3. [ステップ3]

**異常系の挙動**:

| 条件        | 挙動           |
| ----------- | -------------- |
| [異常条件1] | [対応する挙動] |
| [異常条件2] | [対応する挙動] |

**画面遷移**: [遷移先の画面ID / 遷移なし]
**状態変化**: [操作前の状態] → [操作後の状態]

### 操作: [操作名2]

**トリガー**: [ユーザーのアクション]
**前提条件**: [操作が有効な条件]

**正常系の挙動**:

1. [ステップ1]
2. [ステップ2]

**異常系の挙動**:

| 条件        | 挙動           |
| ----------- | -------------- |
| [異常条件1] | [対応する挙動] |

**画面遷移**: [遷移先の画面ID / 遷移なし]
**状態変化**: [操作前の状態] → [操作後の状態]
